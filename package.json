{
  "name": "@rewind/vault",
  "version": "1.0.0",
  "description": "The Vault — password-gated adult catalog/search/scrapers for Rewind, packaged as an operator-installed addon. Resolves streams through The Forge.",
  "license": "UNLICENSED",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/plugin.ts --bundle --format=esm --platform=node --target=node22 --outfile=dist/index.mjs --tsconfig=tsconfig.json"
  },
  "dependencies": {
    "@rewind/forge": "file:../rewind_forge"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
